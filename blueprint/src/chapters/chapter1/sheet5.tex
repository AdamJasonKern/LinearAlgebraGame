\section{NegativesInSubspace}

\begin{theorem}
  \label{theorem : subspace_neg}
  \lean{LinearAlgebraGame.subspace_neg}
  \leanok
  \uses{definition : Vector Space}
  \uses{definition : subspace}
  This is a proof that if a subspace contains a vector `x`, it also contains `-x`: ∀ (x : V), x ∈ W → (-x) ∈ W
\end{theorem}
